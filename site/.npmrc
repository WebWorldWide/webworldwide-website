; eslint-plugin-react and eslint-plugin-jsx-a11y still declare their peer as
; eslint <=9, but they work fine under eslint 10 (verified: `npm run lint` is
; clean). Allow install/ci to proceed despite that lagging peer range until the
; plugins widen it. Remove this once they support eslint 10 officially.
legacy-peer-deps=true
